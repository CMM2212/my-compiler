package compiler.parser.ast.nodes;

/**
 * A node that represents a declaration.
 *
 * Declaration nodes are used to represent the declaration of variables. They extend from
 * Node which allows them to be visited by the ASTVisitor.
 *
 * This interface does not provide additional functionality, but is used for organization/clarity.
 */
public interface DeclarationNode extends Node {
}
